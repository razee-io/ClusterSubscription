const assert = require('chai').assert;

describe('#processSubscriptions', async function () {
  it('should process subscriptions correctly (TEST NOT IMPLEMENTED)', async function () {
    /*
    To fully test, would need to stub out calls to Razeedash-api (getSubscriptionsByCluster) and kubernetes
    */
    assert.equal( 'placeholder', 'placeholder' );
  });

  it('should throw error when invalid subscriptions found (TEST NOT IMPLEMENTED)', async function () {
    /*
    To fully test, would need to stub out calls to Razeedash-api (getSubscriptionsByCluster) and kubernetes
    */
    assert.equal( 'placeholder', 'placeholder' );
  });
});
